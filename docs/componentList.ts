import type { ComponentList } from '../playground'
import Typography from './components/typography.doc'

const componentList: ComponentList = {
  Typography: {
    component: Typography
  }
}

export default componentList
